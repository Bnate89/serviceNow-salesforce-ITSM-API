<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:doc="http://www.mulesoft.org/schema/mule/documentation"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns:apikit="http://www.mulesoft.org/schema/mule/mule-apikit"
      xmlns:secure-properties="http://www.mulesoft.org/schema/mule/secure-properties"
      xmlns:os="http://www.mulesoft.org/schema/mule/os"
      xmlns:api-gateway="http://www.mulesoft.org/schema/mule/api-gateway"
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd
        http://www.mulesoft.org/schema/mule/mule-apikit http://www.mulesoft.org/schema/mule/mule-apikit/current/mule-apikit.xsd
        http://www.mulesoft.org/schema/mule/secure-properties http://www.mulesoft.org/schema/mule/secure-properties/current/mule-secure-properties.xsd
        http://www.mulesoft.org/schema/mule/os http://www.mulesoft.org/schema/mule/os/current/mule-os.xsd
        http://www.mulesoft.org/schema/mule/api-gateway http://www.mulesoft.org/schema/mule/api-gateway/current/mule-api-gateway.xsd">

    <!-- ============================================== -->
    <!-- GLOBAL PROPERTIES                              -->
    <!-- ============================================== -->
    <global-property name="mule.env" value="dev" doc:name="Environment Property"/>
    <global-property name="secure.key" value="${secure.key}" doc:name="Secure Key Property"/>

    <!-- ============================================== -->
    <!-- CONFIGURATION PROPERTIES                       -->
    <!-- ============================================== -->
    <configuration-properties file="config/application-${mule.env}.yaml" doc:name="Application Properties"/>
    
    <secure-properties:config name="Secure_Properties_Config" 
                              file="config/secure-properties-${mule.env}.yaml" 
                              key="${secure.key}"
                              doc:name="Secure Properties Config">
        <secure-properties:encrypt algorithm="AES" mode="CBC"/>
    </secure-properties:config>

    <!-- ============================================== -->
    <!-- GLOBAL CONFIGURATION                           -->
    <!-- ============================================== -->
    <configuration defaultErrorHandler-ref="global-api-error-handler" doc:name="Global Configuration"/>

    <!-- ============================================== -->
    <!-- API AUTODISCOVERY                              -->
    <!-- ============================================== -->
    <api-gateway:autodiscovery apiId="${api.id}" flowRef="api-main-flow" doc:name="API Autodiscovery"/>

    <!-- ============================================== -->
    <!-- APIKIT CONFIGURATION                           -->
    <!-- ============================================== -->
    <apikit:config name="salesforce-servicenow-api-config" 
                   api="api/salesforce-servicenow-api.raml" 
                   outboundHeadersMapName="outboundHeaders" 
                   httpStatusVarName="httpStatus"
                   doc:name="APIKit Router Config"/>

    <!-- ============================================== -->
    <!-- HTTP LISTENER CONFIGURATION                    -->
    <!-- ============================================== -->
    <http:listener-config name="HTTP_Listener_Config" doc:name="HTTP Listener Config">
        <http:listener-connection host="${http.listener.host}" port="${http.listener.port}">
            <reconnection>
                <reconnect frequency="${retry.frequency}" count="${retry.maxAttempts}"/>
            </reconnection>
        </http:listener-connection>
    </http:listener-config>

    <!-- ============================================== -->
    <!-- SALESFORCE HTTP REQUEST CONFIGURATION          -->
    <!-- ============================================== -->
    <http:request-config name="Salesforce_HTTP_Request_Config" 
                         doc:name="Salesforce HTTP Request Config"
                         responseTimeout="${http.request.responseTimeout}">
        <http:request-connection protocol="HTTPS" 
                                  host="${salesforce.api.host}" 
                                  port="${salesforce.api.port}">
            <reconnection>
                <reconnect frequency="${retry.frequency}" count="${retry.maxAttempts}"/>
            </reconnection>
            <http:client-socket-properties connectionTimeout="${http.request.connectionTimeout}"/>
        </http:request-connection>
    </http:request-config>

    <!-- ============================================== -->
    <!-- SERVICENOW HTTP REQUEST CONFIGURATION          -->
    <!-- ============================================== -->
    <http:request-config name="ServiceNow_HTTP_Request_Config" 
                         doc:name="ServiceNow HTTP Request Config"
                         responseTimeout="${http.request.responseTimeout}">
        <http:request-connection protocol="HTTPS" 
                                  host="${servicenow.api.host}" 
                                  port="${servicenow.api.port}">
            <reconnection>
                <reconnect frequency="${retry.frequency}" count="${retry.maxAttempts}"/>
            </reconnection>
            <http:client-socket-properties connectionTimeout="${http.request.connectionTimeout}"/>
        </http:request-connection>
    </http:request-config>

    <!-- ============================================== -->
    <!-- OBJECT STORE CONFIGURATION                     -->
    <!-- ============================================== -->
    <os:object-store name="Case_Incident_Mapping_Store" 
                     doc:name="Case Incident Mapping Object Store"
                     persistent="true"
                     entryTtl="${objectstore.entryTtl}"
                     entryTtlUnit="${objectstore.entryTtlUnit}"
                     maxEntries="${objectstore.maxEntries}"
                     expirationInterval="${objectstore.expirationInterval}"
                     expirationIntervalUnit="MINUTES"/>

</mule>
